<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="./LogoUOL.jpg" type="image/jpg">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <title>Admin Panel</title>
    <style>
        /* Reset Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            height: 100%;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(to bottom, rgba(120, 125, 128, 0.8), rgba(79, 103, 123, 0.8)), url('./academic-block-university-of-layyah1729969953.jpg') no-repeat center/cover;
            color: #333;
            display: flex;
            flex-direction: column;
        }

        /* Header Styles */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: linear-gradient(90deg, #1565c0, #1e88e5);
            color: #fff;
            padding: 1rem 2rem;
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        header h1 {
            font-size: 2rem;
            font-weight: 700;
        }

        nav {
            display: flex;
            gap: 1.5rem;
        }

        nav a {
            text-decoration: none;
            color: #fff;
            font-size: 1rem;
            font-weight: 500;
            padding: 0.5rem 1rem;
            border-radius: 5px;
            transition: background-color 0.3s, color 0.3s;
        }

        nav a:hover {
            background: #ffcc00;
            color: #333;
        }

        /* Toggle Button Styles */
        .toggle-button {
            display: none;
            flex-direction: column;
            gap: 0.3rem;
            cursor: pointer;
        }

        .toggle-button div {
            width: 25px;
            height: 3px;
            background-color: #fff;
            border-radius: 3px;
            transition: transform 0.3s ease;
        }

        @media (max-width: 876px) {
            .toggle-button {
                display: flex;
            }

            nav {
                display: none;
                flex-direction: column;
                gap: 10px;
                background: #1e88e5;
                position: absolute;
                top: 60px;
                left: 0;
                width: 100%;
                padding: 20px;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            }

            nav.active {
                display: flex;
            }
        }

        /* Main Content */
        .container {
            flex: 1;
            max-width: 1200px;
            margin: 40px auto;
            text-align: center;
            padding: 0 20px;
        }

        .university-title {
            font-size: 2.8rem;
            color: #ffffff;
            margin-bottom: 30px;
            font-weight: 700;
            text-shadow: 1px 1px 5px rgba(0, 0, 0, 0.1);
        }

        .links {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .links a {
            background: linear-gradient(to right, #42a5f5, #1e88e5);
            color: white;
            font-size: 1.1rem;
            padding: 15px 20px;
            border-radius: 8px;
            font-weight: bold;
            text-align: center;
            transition: all 0.3s ease;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .links a:hover {
            background: #1565c0;
            transform: translateY(-5px);
            box-shadow: 0 6px 10px rgba(0, 0, 0, 0.2);
        }

        /* Footer Styles */
        footer {
            background: linear-gradient(90deg, #1565c0, #1e88e5);
            color: white;
            text-align: center;
            padding: 15px;
            box-shadow: 0 -4px 10px rgba(0, 0, 0, 0.1);
        }

        footer small {
            font-size: 0.9rem;
        }

        /* Subtle Animations */
        header, .container, footer {
            animation: fadeIn 1.2s ease;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Media Queries for Footer Adjustment */
        @media (max-width: 768px) {
            footer {
                padding: 10px;
                font-size: 0.8rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <h1>Admin Panel</h1>
        <button class="toggle-button" id="toggleButton">
            <div></div>
            <div></div>
            <div></div>
        </button>
        <nav id="navMenu">
            <a href="/about">About</a>
            <a href="/login">Student Login</a>
            <a href="/professorlogin">Professor Login</a>
        </nav>
    </header>

    <!-- Main Content -->
    <div class="container">
        <h1 class="university-title">Attendance System</h1>
        <div class="links">
            <a href="/register">Register New Students</a>
            <a href="/professorregister">Register New Professors</a>
            <a href="/professorChoice">See All Professors List</a>
            <a href="/feedbackRead">See Feedback of the Peoples</a>
            <a href="/login">Student Login</a>
            <a href="/professorlogin">Professor Login</a>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <small>
            &copy; <script>document.write(new Date().getFullYear())</script> Attendance System. All Rights Reserved.
        </small>
    </footer>

    <!-- JavaScript for Toggle Menu -->
    <script>
        const toggleButton = document.querySelector('.toggle-button');
        const navMenu = document.getElementById('navMenu');

        toggleButton.addEventListener('click', () => {
            navMenu.classList.toggle('active');
        });
    </script>
</body>
</html>
